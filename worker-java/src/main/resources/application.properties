redis.host=${REDIS_HOST:localhost}
redis.port=${REDIS_PORT:6379}
redis.queue=${REDIS_QUEUE:scan-jobs}
redis.dlq=${REDIS_DLQ:scan-dlq}
worker.threads=${WORKER_THREADS:4}
worker.blpop.timeout=${WORKER_BLPOP_TIMEOUT:5}
db.url=${DB_URL:localhost}
db.username=${DB_USERNAME:postgres}
db.password=${DB_PASSWORD:postgres}
groq.api.key=${GROQ_API_KEY:123456723456789}
anthropic.api.key=${ANTHROPIC_API_KEY:poiuytrewertyuio}
grok.api.key=${GROK_API_KEY:poiuytrertyui}
gemini.api.key=${GEMINI_API_KEY:jhgfdsdfghj}
GITHUB_TOKEN=ghp_yourtoken
ANTHROPIC_API_KEY=sk-ant-...

anthropic.api-key=${ANTHROPIC_API_KEY}
# redis.host=${REDIS_HOST}
# redis.port=${REDIS_PORT}
# redis.password=${REDIS_PASSWORD}
# redis.dlq=${REDIS_DLQ}
github.token=${GITHUB_TOKEN}

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
